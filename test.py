from config.config import BASE_DIR
from pathlib import Path

from config.json_init import JsonAddConfig

from agent.config_IA import (
    NameSetter
    )

config_name = BASE_DIR / "config" / "agents.json"
chat_name_df = "new chat"



